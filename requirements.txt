diffusers>=0.37.0
fonttools>=4.56.0
gradio>=5.47.2,<=5.50.0
manga-ocr>=0.1.14
numpy>=1.24.0
opencv-contrib-python>=4.8.0
packaging>=24.0
pillow>=11.1.0
pyoxipng>=9.1.1
pythainlp>=5.3.4
requests>=2.32.3
safetensors>=0.4.0
scikit-learn>=1.3.0
scipy>=1.10.0
sdnq>=0.1.3
skia-python>=87.7
spandrel>=0.3.0
torch
torchvision
transformers>=5.0.0
uharfbuzz>=0.48.0
ultralytics>=8.3.94
# surya-ocr is intentionally NOT listed here. Every 0.14.0-0.17.1 release
# (the entire pre-"Surya 2" line - 0.18.0/0.19.0 were never published, the
# next release after 0.17.1 is 0.20.0) pins pillow<11.0.0 / pillow>=10.2.0,
# which permanently conflicts with this file's pillow>=11.1.0 (needed by
# gradio and others). 0.20.0+ ("Surya 2") requires a running vllm or
# llama.cpp inference-server backend, unsuitable for a CPU-only Actions
# runner, so upgrading past 0.17.1 is not an option either.
# Fix: install surya-ocr with `pip install --no-deps "surya-ocr>=0.14.0,<0.18.0"`
# in the CI workflow (see .github/workflows/bot.yml), so pip's resolver never
# sees the surya-ocr <-> pillow>=11.1.0 clash. Because --no-deps skips ALL of
# surya-ocr's declared deps, the workflow also runs a verification step
# (`python -c "import surya..."`) right after install and installs anything
# it's missing - see the "Clone & Setup MangaTranslator Core" step for the
# up-to-date, actually-tested list of extra packages surya-ocr needs. Do not
# hand-copy a dependency list here without cross-checking it against that
# step; a stale/guessed list will silently break at import time instead of
# at pip-install time.
# paddleocr: classic PP-OCRv4 OCR (paddleocr-classic method), NOT the
# PaddleOCR-VL VLM used by the paddleocr-vl-1.6 method. Also requires the
# hardware-specific `paddlepaddle` package (CPU/GPU build), which - like
# torch - is intentionally not listed here and must be installed separately
# for the target hardware (see the bot repo's CI workflow for the CPU
# install command).
paddleocr>=2.7.0
