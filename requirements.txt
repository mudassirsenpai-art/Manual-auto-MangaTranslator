diffusers>=0.37.0
fonttools>=4.56.0
gradio>=5.47.2,<=5.50.0
manga-ocr>=0.1.14
numpy>=1.24.0
opencv-contrib-python>=4.8.0
packaging>=24.0
pillow>=11.1.0
pyoxipng>=9.1.1
pythainlp>=5.3.4
requests>=2.32.3
safetensors>=0.4.0
scikit-learn>=1.3.0
scipy>=1.10.0
sdnq>=0.1.3
skia-python>=87.7
spandrel>=0.3.0
torch
torchvision
transformers>=5.0.0
uharfbuzz>=0.48.0
ultralytics>=8.3.94
# surya-ocr: pinned below 0.18 to stay on the pre-"Surya 2" API
# (FoundationPredictor/RecognitionPredictor/DetectionPredictor), which runs
# purely on local torch. 0.18+ ("Surya 2") requires a running vllm or
# llama.cpp inference-server backend, which is unsuitable for a CPU-only
# Actions runner - do not upgrade past this without re-checking that.
surya-ocr>=0.14.0,<0.18.0
# paddleocr: classic PP-OCRv4 OCR (paddleocr-classic method), NOT the
# PaddleOCR-VL VLM used by the paddleocr-vl-1.6 method. Also requires the
# hardware-specific `paddlepaddle` package (CPU/GPU build), which - like
# torch - is intentionally not listed here and must be installed separately
# for the target hardware (see the bot repo's CI workflow for the CPU
# install command).
paddleocr>=2.7.0
